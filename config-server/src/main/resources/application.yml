spring:
      application:
                 name: config-server
      profiles: 
                active: native
      cloud:
           config:
                 server:
                       native:
                            searchLocations: file:..\\..\\..\\..\\configs

server:
      port: 0

eureka:
    instance:
         instanceId: ${spring.application.name}:${random.uuid}